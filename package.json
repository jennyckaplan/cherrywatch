{
  "scripts": {
    "test": "jest",
    "update": "jest -u"
  },
  "devDependencies": {
    "@types/jest": "^27.4.1",
    "@types/jest-image-snapshot": "^4.3.1",
    "jest": "^27.5.1",
    "jest-image-snapshot": "^4.5.1",
    "puppeteer": "^13.5.2"
  },
  "jest": {
    "setupFilesAfterEnv": [
      "<rootDir>/jest-setup.js"
    ]
  }
}